import { Page, Text } from '../.././lib';

export default function ManageTransactions() {
  return(
    <Page id='manageTransactions' title='Manage Transactions'>
      <Text label='This is manage transaction page.' />
    </Page>
  );
}
