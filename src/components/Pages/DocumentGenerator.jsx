import { Page, Text } from '../.././lib';

export default function DocumentGenerator() {
  return(
    <Page id='documentGenerator' title='Generate a Document'>
      <Text label='This is generate document page.' />
    </Page>
  );
}
